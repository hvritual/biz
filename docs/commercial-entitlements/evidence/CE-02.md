# CE-02 执行回执

## 范围与结果

完成模块目录、技术状态与依赖管理。实现已由 PR #21 集成 main，并在业务提交 ed73396d5205bb2e12c5c6bb2cb18b36bcba5424 上完成真实资格验证。本次收尾补齐此前缺失的回执与 DONE 台账，不重写已通过验证的业务实现。不实现订阅、价格、支付、Vue 页面或运行时权益拦截；后者分别由后续 CE 任务负责。

## 基线与依赖

- 实施基线：biz main@47e5e90f4ec86ea5b0db0f6442aa00f10730a31d。
- 固定框架：yunka.io@6ba99c1440dc6c9416f6afd08f3282e35fa5a3fb，未升级或修改框架。
- CE-01 DONE：evidence/CE-01.md、evidence/CE-01-verification.json；业务集成 5df140b627aa6fe2868078e8773b5f1a3558993f。
- 无外部资格依赖。前端候选未合并。

## 变更清单

- contracts/proto/commercial/v1/module.proto：7 个平台管理操作；登记 contracts/sources.json。
- internal/commercial/modulecatalog：代码声明的能力、额度、字段策略与依赖；模块商业元数据、技术/销售状态、版本、幂等、审计和历史编码保护。
- internal/commercial/modulecatalog/migrations/0001_module_catalog.sql：5 张显式迁移表。
- internal/commercial/application/module_catalog.go：ModuleCatalog Application；internal/bizruntime/runtime.go 接入 generated assembly 工厂。
- PB Go、OperationPlan、REST/gRPC、OpenAPI、client.ts、assembly 等产物由生成器产生，未手改。
- integration/ce02_module_catalog_mysql_test.go 及 Registry/平台授权反例测试。
- .github/workflows/ce02-bootstrap.yml：固定来源、无漂移生成、真实 MySQL 和全量回归。
- 本次新增轻量 ce-round-receipts.yml，检查所有 DONE 任务的真实主线祖先与 --require-main 条件，保留执行日志和被验证源码快照。

## 验证命令与结果

主线 run：https://github.com/hvritual/biz/actions/runs/34310754865 ，job 102336754451，SUCCESS，2026-09-09 UTC。环境 Ubuntu 24.04、Go 1.25.13、MySQL 8.4；来源为上述固定 SHA。

| 命令/检查 | 退出码 | 测试/结果 | 日志 |
| --- | --- | --- | --- |
| python3 docs/commercial-entitlements/tools/check_plan.py | 0 | 计划结构通过 | plan.log |
| make yunka-source-check workspace-check | 0 | 固定来源解析通过 | resolution.log |
| make check | 0 | 前后均通过 | check-before.log / check-after.log |
| make generate，连续两次，git diff --exit-code 与空 status | 0 | 两次均无漂移 | generate-1.log / generate-2.log |
| ModuleCatalog 产物/迁移检查 | 0 | 7 operations，REST/gRPC，5 tables | contract.log |
| go test -count=1 -json ./internal/commercial/... -run TestCE02 | 0 | 2 叶子测试，0 失败 | unit.jsonl |
| go test -count=1 -tags=integration -json ./integration -run '^TestCE02' | 0 | 2 叶子测试，0 失败 | mysql.jsonl |
| go test -count=1 -json ./... | 0 | 29 叶子测试，31 pass 事件，0 失败 | go-test.jsonl |
| go vet ./...；go build ./... | 0 | 通过 | 上述 Actions job 的 Go regression step |

测试套件存在包含关系，不将 focused 与全量数量相加作为独立测试数。原资格包中的 round_check=SKIPPED_NOT_DONE 是当时真实状态，不改写为 PASS；本次证据/台账提交后，由主线回执工作流实际执行 check_round。

证据 artifact 10088297028，名称 ce02-ed73396d5205bb2e12c5c6bb2cb18b36bcba5424。ZIP SHA256：34d8f0f9b51637034d51c48107b0ba2cfd38cbab6ccff54d130397a4dea454d8。已下载核对 summary.json 的 11 项文件哈希与 JSONL 计数。机器回执见 CE-02-verification.json。

## 反例与故障验证

真实 MySQL 测试覆盖过期版本、重复请求、操作/模块幂等键冲突、依赖未创建、未实现模块设为 ready、引用中模块删除、删除重试和历史编码复用；验证审计 actor/before/after/reason。Registry 单测覆盖缺依赖/环；平台授权测试证明租户主体不能执行平台创建操作。

实施中实际失败：source inventory 遗漏、测试误用不存在的 ModuleCatalogResolver、runtime 未实现新增的 BuildCommercialModuleCatalog。均按真实生成契约修复并通过复验，没有降低门禁。未发现必须修改框架的缺口。

## 风险与回滚

CE-02 是目录管理，不等于租户商业权益拦截。销售停售与技术停用独立。Registry 定义随发布受控，不能由管理员任意创建 Operation。后续套餐/授权引用需要在各自任务接入引用保护，不宣称当前已覆盖尚未实现的订阅对象。

回滚先撤出新增管理入口或回退商业元数据配置；不得删除被引用目录或复用历史编码。新增迁移保持向前兼容；生产回滚/备份恢复演练尚未执行，不将代码验证当作生产演练。

## 集成与回读

PR：https://github.com/hvritual/biz/pull/21 。业务集成和主线验证 SHA 均为 ed73396d5205bb2e12c5c6bb2cb18b36bcba5424。候选 push run 34310588899、PR run 34310592660，以及同候选 B12/C9 门禁均成功；主线 run 34310754865 成功。收尾前回读 main 仍为该业务 SHA、tasks.json 为 PLANNED，故本次只追加真实证据并修复台账漂移。证据提交由 Git 历史标识，不在自身内容写入自引用 SHA。

## 自检

scope：仅 CE-02 实施回执及完成门禁；dependency：CE-01 已完成且证据可读；quality：真实 MySQL、生成无漂移及全量 Go 回归通过；risks：未冒充运行时授权/生产资格；rollback：保留数据与历史编码，生产演练未执行。
