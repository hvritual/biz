# CE-04 执行回执（BLOCKED，未完成）

## 范围与结果

专项授权与确定性权益解析代码及生成契约保存在 PR #25 / feat/ce-04-entitlements，当前已审阅源提交 437b0f052e823fdf99dfa3cdda2104b25c7b7627。实现包含创建／撤销／列表／平台解释／租户自查询五个 API、持久化来源、类型化数量与字段动作、有效期、拒绝优先、来源解释、版本冲突、幂等与同根事务审计。

CE-04 未独立闭环，不是 DONE；没有合并 main，没有最终主线资格或 --require-main 结果。现有外部阻塞不能用旧版本测试替代。本回执和机器记录明确 result=BLOCKED，tasks.json 改为 BLOCKED，integration_commit 保持 null。

不实施 CE-05 Guard、CE-06 快照／业务撤权屏障、订阅、支付、前端、额度占用或实际字段输出过滤。计划／附加包仅有类型化接口，生产没有假来源。

## 基线与依赖

main 9ea878d72650216a26c39eddf98374717f2a562b；Yunka 6ba99c1440dc6c9416f6afd08f3282e35fa5a3fb。CE-01/02/03 的已完成状态保持，CE-05 起未提前启动。

当前源 tree e0ade58957fb636fbebcb37323d11a229acaeb68；下载 preparation artifact 10094218893 后在本地重新 git write-tree，结果与远端一致。该 source ZIP SHA256 为 2aa89887fa7bf2a7426c788af09ddb0bb55c3081e29609d05021b34f76e18c61，不把准备工作流成功当成最终 MySQL／主线资格。

## 变更清单

entitlement.proto、EntitlementManagement owner 的 nested usecase、commercial/domain/entitlement、ports 与 scoped persistence、显式迁移、ModuleCatalog typed read child、runtime factory、商业 mapping v2 和自动生成产物；纯解析、架构、真实 REST/gRPC/MySQL、重启及异常测试。

完整契约与合并规则见 ../decisions/CE-04-entitlement-sources.md。查询采用受支持的 POST body:* 只读操作，不在生成器遗漏 repeated GET query 时手工改 transport。框架缺口单独登记 yunka.io #177。

新增源码校验保证来源目标必须在当前目录中存在；授权历史通过外键保持模块引用；撤销不删除历史。运行时清单比较 helper 与9个测试已存在，但未接入被拒修改的旧 B12.7 workflow。

## 验证命令与结果

### 当前源的本地预检，不是最终资格

在 2026-09-09 UTC，对源提交 437b0f... 的逐字 tree 快照执行：

- GO111MODULE=off GOTOOLCHAIN=local GOWORK=off go test -count=1 -json ./internal/commercial/domain/entitlement：45个叶子／51个 pass 事件，0失败，退出0。
- PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts -p test_ce04_runtime_closure.py -v：9测试通过，退出0。
- PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s docs/commercial-entitlements/tools -p 'test_*.py' -v：8测试通过，退出0。
- PYTHONDONTWRITEBYTECODE=1 python3 docs/commercial-entitlements/tools/check_plan.py：结构验证通过，退出0。

本地 Go 是1.23.2，不是锁定1.25.13；标准库领域测试以 GO111MODULE=off 独立运行，没有修改仓库 go.mod。锁定工具链自动获取因运行环境无法解析 proxy.golang.org 失败；这不是 Go 全项目编译成功。没有把本地预检声称为锁定工具链、真实数据库、完整运行时或生产认证。

### 已下载并核验的历史资格，不能覆盖当前修复

首版候选 a72119b7bd00e32abb840f3c93ed486fc8b004dd 的 CE-04 run 34324260783 成功，实际测试的是 PR merge ref ebcc75f33c53ebbd0fb5ff9b03221bf13858293b。artifact 10093147186，ZIP SHA256 f5e0ea54eadd98d226509eac83600f55b9158e775edd39412c4ca611e9cb5dda。已重新核对20项日志哈希。

其结果为 focused43、全量Go112叶子、MySQL4、真实数据库重启前后各1、CE-02 MySQL2；套件重叠不相加。restart.log 记录 workflow-owned MySQL 容器 StartedAt 从 07:31:37.784441982Z 变为 07:32:59.123489991Z，重启后读回通过。历史 round_check=QUALIFIED_NOT_DONE 保持不变。

之后的 POST 契约、来源引用与永久回归修正改变了源码，所以历史成功不能成为当前源的最终 PASS。当前源 PR 资格 run 34327172671 被 GitHub 标记 action_required，其他相关检查也未形成新的最终通过证明。

## 反例与故障验证

当前纯解析预检覆盖 A/B来源隔离、无隐式免费、模块及能力显式拒绝、安全拒绝、技术停用、停售保留权益、半开区间、未来生效、撤销、依赖缺失／循环、字段动作／脱敏、额度零／无限／溢出／替换／重叠以及非法来源目标。真实数据库并发、幂等、审计回滚和重启有首版历史记录，修复后的最终复验仍须完成。

旧 B12.7 run 34324260641 确实失败；其 artifact 10093126000（ZIP SHA256 17695c8b33f634f29245376980a1ae228aa9652387f4e63ee37565f884930589）保留实际 runtime 状态。该 workflow 将 graphNodes/runs 清单写死为六个，当前声明和装配有八个真实 Application。

## 风险与回滚

阻塞已登记 biz #26：B12.7 应按生成 contract manifest 严格校验完整应用、运行边和路由，不是把6机械改8或减少应用。该 workflow 修改此前被权限／安全检查拒绝，尚未获准执行；本次没有重试该被拒写入、绕过批准、更换权限、改成功状态或降低门禁。

另需 GitHub 正常工作流审批处理 bot-generated 源提交的 action_required。发现源码分支已有后续修复时，旧树非快进更新被拒绝，本次保留实际远端最新提交，没有 force push。

代码尚未进入 main，未影响主线运行。以后授权上线的回滚应通过 revoke 新版本保留审计；没有执行生产回滚演练。必须先正常处理阻塞、完整复验后才可合并，不以已有部分测试替代。

## 集成与回读

PR #25 保持未合并；主线仍为 9ea878d72650216a26c39eddf98374717f2a562b。此记录只保存当前任务阻塞和证据，不宣称本轮达到独立完成标准。后续状态以实际候选和 main 的重新回读为准，不回写历史失败记录为成功。

附件 CE-04-implementation-blocked-evidence.zip 保存当前源码快照、首版资格与失败原始证据、本地预检日志和哈希索引。机器记录见 CE-04-verification.json。

## 自检

scope：只 CE-04 与其阻塞记录；dependency：CE-02 已完成；quality：区分当前局部预检与旧候选完整资格；risks：审批和旧运行时门禁仍未解除；rollback：主线未合并，不删除历史审计。总体结果 BLOCKED，不是独立完成。
