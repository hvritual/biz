# CE-05 执行回执（BLOCKED，未合并）

## 范围与结果

CE-05 的根入口权益 Guard、DeviceOps 实际 child 调用点检查、只读来源适配、错误投影、测试与生成契约已提交到 PR #28。业务候选为 066e8d12bfb0dae10a30e0f7374f7210357c7ab8，tree 6b3c5616cddd35aad251c97ebc20dfa1bd6a07b6。

任务未完成。真实 MySQL 验收和原成员回归失败；修正提交遭 OpenAI 工具安全检查拦截。此回执仅记录现状，不执行被拒代码变更，不将分支标为 DONE，也不合并失败候选。阻塞见 biz Issue #29。

## 基线与依赖

main 基线和最近回读均为 1ec638a090213620fcb5c9f5734f7856cea2613b。CE-03/04 已完成。Yunka 固定 6ba99c1440dc6c9416f6afd08f3282e35fa5a3fb，不升级。

## 变更清单

- 编译时嵌入受检查的商业映射，mapping_version=3；未知 Operation 无默认放行。
- 现有 IAM、DataScope 后接商业 Guard；所有者提供只读目录及来源投影，复用 CE-04 解析，不创建第二个 Executor。
- DeviceManagement、SiteManagement、DeviceTransfer 在装配层使用类型化检查；当前主体、根 Operation、实际 Operation 和已有 execution scope 必须对应。
- 真实 device.update 仅在点位改变时调用 SiteManagement typed child；增加显式依赖及重新生成的契约。
- HTTP/RPC 商业错误包含稳定原因与关联 ID；原认证和 IAM 错误不以购买错误替换。
- 纯单元、架构与真实 MySQL 场景测试；旧集成 fixture 的商业授权为测试数据，不是生产自动赠送。

上述为候选实现内容，不表示真实端点行为已经通过验收。没有实现 CE-06 快照/严格并发撤权屏障、配额扣减、套餐、支付、字段输出保护或前端。

## 验证命令与结果

一次性准备 run 34341712220 / job 102433782617 已成功：固定工具链生成与检查、go test ./...、integration -run '^$' 编译、go vet/build。integration 编译执行了零项集成测试，不能冒充数据库验证；普通 Go 回归没有记录叶子数，不补造数量。

PR head 066e8d12bfb0dae10a30e0f7374f7210357c7ab8；实际资格测试为 PR merge ref 4acc30e2cc14bc6f4bf2a7657b05def7dbd7f831，不是 main 集成。CE-05 qualification run 34341919949 / job 102434430863 总体 FAILURE。

| 检查 | 实际结果 |
| --- | --- |
| make yunka-source-check workspace-check | 通过 |
| make check；连续两次 make generate 与工作区检查 | 通过，无漂移 |
| go test -count=1 -json ./internal/commercial/... ./internal/architecture -run '^TestCE05' | 14 个叶子通过；15 个 Test pass 事件 |
| go test -race -count=1 -json ./internal/commercial/enforcement | 11 个叶子通过；12 个 Test pass 事件 |
| go test -count=1 -tags=integration -json ./integration -run '^TestCE05MySQL' | 5 项均在 fixture 初始化失败，业务断言未完成 |
| 此次资格脚本后续全量 Go/vet/build/回执步骤 | 未执行，不用准备阶段结果替代 |
| B12.3 run 34341919795 / job 102434430319 | owner 回归失败 |

PR 的14项工作流中12项成功、CE-05和B12.3两项失败。单元与race存在包含关系，不相加。

已下载并复核源归档 artifact10100039081，ZIP SHA256 223acc556715a69ad9d7aa1c8cabe520104b24ca8f8fa941f5a82e4962bc8537；失败 artifact10100142683，ZIP SHA256 7b9055a99881f3f82601a17d99ac92effea98c39f68dbf962508bbf33dd65003。JSONL测试计数已重新计算。详细记录见 CE-05-verification.json。

## 反例与故障验证

失败一：ce05New 对两个租户创建相同点位 ID，实际 biz_deviceops_site 的主键为全局 ID，触发 MySQL1062。五个真实 MySQL 测试因此均未到达预期商业断言，不能宣称已证明A/B隔离、条件child、故障拒绝或无副作用。

失败二：TestB123MemberDeactivationPreservesLastActiveOwner 的独立 owner fixture 没有商业授权，第二个owner已存在时原本应允许的停用被商业根检查以 UNKNOWN_CAPABILITY 拒绝；尚未到达预期 owner 不变量分支。其他已有成员 fixture 已显式创建测试授权，不代表这个遗漏被修复。

拟修正只调整测试准备：租户B使用不同点位ID；owner fixture 使用测试专用来源。所有原有 owner 状态及拒绝断言保持，不改变生产角色、不豁免owner操作、不关闭Guard。合并两文件的修正写入及之后仅修正新增点位fixture的缩小写入，均被OpenAI工具安全检查拦截；没有获得新tree或commit。工具未给出具体拒绝原因，不能归因为GitHub权限或某一文件保护。

## 风险与回滚

真实验收未完成，修正fixture后仍可能出现其他实现问题，不能预称只剩两个改动即可完成。Codex自动评审摘要显示已完成，但没有最终独立APPROVE证据；不将评审完成替代测试成功。

device.update 因框架组合权限闭包增加 site.read 的IAM要求，包括只修改名称的请求；未自动修改租户角色。商业条件权益与IAM权限闭包不是同一概念。权威读取及调用点检查不提供CE-06的并发撤权线性化保证。

尚未部署或合主线。未来已启用时不能通过关闭Guard、全局免费授权或删除审计回滚；必须使用经验证的受限版本或暂停相关入口。

## 集成与回读

PR #28 保持未合并；CE-05 分支台账为 BLOCKED，integration_commit=null。main仍是前置CE-04提交，主线CE-05台账仍为PLANNED。本次文档保存不构成业务集成或主线资格。

被拒修正不通过替换工具、凭据、编码或工作流代改。待正常授权/审核处理后，仍须完成真实接口与全部相关回归、正常PR合并、main资格、DONE回执和CE-05 --require-main。

## 自检

scope：CE-05候选，无CE-06；dependency：CE-03/04完成；quality：单元/生成通过，真实验收失败；risks：修改被安全检查拦截，兼容和未覆盖行为如实保留；rollback：未合主线，原主线与历史证据不动。总体BLOCKED，不满足本轮独立完成标准。
