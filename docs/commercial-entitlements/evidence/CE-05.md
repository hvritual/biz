# CE-05 执行回执

## 范围与结果

CE-05 根入口与实际 child 调用点的服务端权益限制已通过 PR #28 合入 main。
业务集成及验证提交：`1991514be0a8c65996bdb27a2d4f9680aba32667`。
本完成轮修正两处集成测试准备，并完成实际接口与相关回归；不扩大到 CE-06。
机器证明见 `CE-05-verification.json`，历史 BLOCKED 记录仍可从原分支提交回读。

## 基线与依赖

本轮开始 main：`1ec638a090213620fcb5c9f5734f7856cea2613b`。
CE-03、CE-04 已完成；Yunka 固定 `6ba99c1440dc6c9416f6afd08f3282e35fa5a3fb`，没有升级。
原候选：`066e8d12bfb0dae10a30e0f7374f7210357c7ab8`。
测试修正：`63fe41f71d67e669cc70534880f6b472f271614a`。

## 变更清单

根入口使用已校验商业映射及 CE-04 权威来源解析；保留已有 IAM 与资源 DataScope。
DeviceManagement、SiteManagement、DeviceTransfer 的类型化调用点校验真实主体、租户、根执行范围与实际 Operation，防止直接调用绕过。
真实 device.update 只有改变点位时调用目标点位 child；商业迁移能力只在实际需要时检查。
HTTP/gRPC 返回稳定商业拒绝原因和关联 ID；认证与 IAM 错误保持原语义，决策日志提供审计关联。
具体实现和兼容说明见 `../decisions/CE-05-server-enforcement.md`。

本完成轮只修正两个测试文件：`integration/ce05_fixture_test.go` 为租户 B 创建独立全局点位 ID；`integration/b12_member_owner_invariant_mysql_test.go` 为独立 owner 测试显式准备 access-management 权益。
原有 owner 拒绝、状态、版本和成功场景断言均保留。生产 Guard、角色、数据库唯一性及工作流门槛未修改。

## 验证命令与结果

固定 Go 1.25.13、Yunka 和工作流自有 MySQL 8.4，运行于 GitHub Actions。
修正后 PR 的14项相关工作流全部成功；实际候选测试 merge-ref 为 `b87916b462881671fc9c980824178ca0e2c3f008`。
业务主线的8项工作流全部成功。主线 CE-05 qualification：run `34345367092`，job `102445582819`；B12.7：run `34345367110`。

| 检查 | 实际结果 |
| --- | --- |
| 固定来源和依赖、make check | 通过 |
| 连续两次 make generate 及 Git 工作区检查 | 通过，无漂移 |
| TestCE05 专项 | 14个叶子用例通过，15个pass事件 |
| 权益限制 race 检测 | 11个叶子用例通过，12个pass事件 |
| TestCE05MySQL | 5项通过，真实 REST/gRPC/DB 断言完成 |
| 全量 Go | 130个叶子用例通过，141个pass事件 |
| go vet、go build | 通过 |
| 计划与回执检查器 | 8项通过 |
| 结果汇总器 | 6项通过 |
| 运行时门禁 | 25项通过；8个应用、28条路由匹配，正常停止 |

以上命令退出码均为0，完整命令与日志定位见机器证明。专项、race、全量套件有包含关系，数量不相加；实际测试失败和跳过为0，无测试文件包单独统计。

主线 CE-05 artifact `10101525909`：ZIP SHA256 `e2a4fa068db33a10f8ab41c5ee197c403e00b695a214f4b3ef65fe0ae5193b41`。
B12.7 artifact `10101507254`：ZIP SHA256 `ab9ffa9850fa0ea7507c3a85978bdf075dbd61cb822e9969b6f3ae1447685b2d`。
两份原始包已下载；CE-05的16项文件哈希和4组JSONL用例计数重新核对一致；runtime以归档的manifest/config/status/graph/diagnostics再次对照通过。

## 反例与故障验证

5项真实集成测试分别覆盖：REST/gRPC 的 A/B 开通与拒绝；只有实际迁移才需要的条件 child；已购租户仍受 IAM/DataScope 限制及原始 Application/child 调用拒绝；技术停用、停售不撤权、到期和来源故障；owner无商业授权与过期授权拒绝。
拒绝写入后检查设备数量、点位、名称和版本，验证没有副作用；平台主体及非法凭据不能借租户 header 越权。
B12.3 的原 owner 场景在准备权益后通过，最后一位 owner 仍不能停用或移除，存在第二位 owner 时按原规则成功。

此前失败不是有效的业务验收：五个新DB用例曾因全局点位ID重复而在准备阶段触发1062；owner测试曾因缺商业来源提前被拒。失败日志及工具拒绝保留于历史提交 `0346550104697900a378a0a349dd02e2078507ce` 和 Issue #29，本轮没有改写历史证据。

## 风险与回滚

`device.update` 的固定框架 IAM 权限闭包要求 `device.update + site.read`，包括仅改名；生产角色未自动调整。上线前须核对真实授权与角色职责，未授权租户的商业业务会被拒绝。
本任务不提供 CE-06 的严格并发撤权序列保证，不执行配额扣减、套餐、支付、字段输出过滤或 Vue。
没有执行生产部署或生产回滚演练。未启用版本可整体撤回；已启用后必须维持限制，恢复经验证的受限版本或暂停相关入口，不能关闭Guard、默认免费开通或删除审计来回滚。
自动评审摘要完成不是独立 APPROVE；本回执不作此声明。

## 集成与回读

PR #28 已正常合并，业务提交与机器证明 verified_commit 一致。
本次三份文档更新将 CE-05 台账设为 DONE，清除旧阻塞并关联实际主线证明。CE-06及后续仍为PLANNED。
业务验证发生在本回执之前，所以其原始summary仍保留 task_status=BLOCKED 和 QUALIFIED_NOT_DONE；它证明业务资格，不预填最终完成结果。
回执合并后在最终main执行 `check_round.py --task CE-05 --require-main`，结果由该主线Actions日志另行记录，不把证据文档自身提交SHA写入自身造成循环。

## 自检

scope：CE-05及两处测试准备；dependency：CE-03/04完成；quality：候选及业务主线实际通过；risks：IAM兼容、无生产部署、无CE-06屏障；rollback：保留限制与历史证据。后续任务未启动。
