# CE-06 执行回执

## 范围与结果

完成 R2/CE-06 权益快照、时间边界、撤权与并发屏障。承接已有 PR #31 的实现，完成真实产物核验和 main 集成；本回执提交仅更新本文件、机器回执与 tasks.json，不追加业务范围。

商业实现已经进入 main，且在实际业务提交上通过专项和回归测试。任务状态据此登记 DONE；最终证据提交仍须在同轮通过主线资格与 `--require-main`，不能将本文件写入本身当成检查已执行。原始产物中的 IN_PROGRESS / QUALIFIED_NOT_DONE 是证据生成时的真实状态，保留不改写。

不做 CE-07 套餐、额度扣减、支付、Vue、框架升级或生产部署。没有引入第二套 Executor、免费权限或可编辑派生快照。

## 基线与依赖

实现基线 biz：`e8a4301ac34bd780be15ba3ed83b35e823015dc2`。固定 Yunka：`6ba99c1440dc6c9416f6afd08f3282e35fa5a3fb`。

CE-05 依赖为 DONE，业务集成 `1991514be0a8c65996bdb27a2d4f9680aba32667`；对应 CE-05.md 与 CE-05-verification.json 已在基线可读。CE-06 无外部凭据门槛；其他任务未满足的外部条件没有删除或改成满足。

## 变更清单

不可变权益快照与当前指针、来源版本及目录 epoch；DB UTC 时间边界；仅版本化派生值缓存及缓存禁用开关；主体特定 permission_version 关联；DeviceOps、成员、角色和实际 child 调用点的根事务屏障。契约与生成物、显式 SQL 迁移、架构反例、MySQL 并发与重启测试随 PR #31 一并集成。详见 `../decisions/CE-06-versioned-snapshots.md`。

## 验证命令与结果

执行环境为 GitHub Actions，数据库为工作流独占 MySQL 8.4；使用仓库锁定的 Yunka 工具链。当前交互容器无法解析 GitHub 主机，未冒充在该容器执行了全量 Go/MySQL 验证；实际运行来自下述云端任务，产物已下载并独立核对。

业务 main：`e5ce55414dde72c61f639ad3e0ffad7b86d46816`。

资格 run `34367964370`，job `102521497221`，2026-09-09 15:05:41Z 发起，15:08:37Z 完成。artifact `10110775443`，ZIP SHA256 `97cfc1056adda10e33a952c7b4667f0f9a019cd49f2457f183ae7011e4149310`。23 个文件哈希匹配，全部套件计数由原始 JSONL 重新计算。

| 实际命令/套件 | 叶子用例通过 | 失败/跳过 |
| --- | ---: | --- |
| CE-06 focused | 17 | 0 / 0 |
| snapshot/consistency/persistence race | 16 | 0 / 0 |
| TestCE06MySQL | 11 | 0 / 0 |
| 数据库重启前 / 重启后 | 1 / 1 | 0 / 0 |
| CE-05 / CE-04 / CE-02 MySQL 回归 | 5 / 7 / 2 | 0 / 0 |
| `go test -count=1 -json ./...` | 147 | 0 / 0 |

套件有重叠，不能相加；父子 pass 事件不是独立用例数，无测试包不等于跳过测试。`make check`、两次 `make generate` 后零漂移、source/workspace 锁定、`go vet ./...`、`go build ./...` 均退出 0。计划回执反向测试 8 项、JSONL 证据反向测试 6 项、运行时闭环反向测试 25 项均通过。精确命令、退出码、测试数、日志定位见 CE-06-verification.json。

业务 main 触发的 8 项工作流全部 success。运行时资格 run `34367964328` / artifact `10110733020` 已下载核验，ZIP SHA256 `928add8d4dbd25104cf21fdb3443e51bef34a1bb86a395b933de0c60d1270b26`；生成契约、配置、进程、图和诊断一致：8 个 Application、28 条路由，ready 与正常 stop 均有记录。

## 反例与故障验证

真实 MySQL 覆盖旧 RR 视图、写先取得屏障/撤权先提交、全局停用的两种顺序、预检后过期、时间边界无需调度、独立实例与旧缓存、权威状态缺失/读故障、来源与快照原子回滚、成员撤权和实际数据库重启。`TestCE06MySQLDeadlockRetriesWholeRequest` 日志确实出现 MySQL 1213，验证根事务失败后整请求重试，不在已失败的根事务内重试单条 SQL。

PR 初次回归暴露旧 CE-05 故障探针仅覆盖 Row 回调，不能命中新 Query 锁定读取。提交 `074fa8cad39891afd41cabdd7910448e0f99541a` 同时覆盖 Row/Query 并断言注入次数大于零；保留既有拒绝类型及接口断言。随后 PR 14 项工作流与业务主线复验通过，没有以删用例或降低断言消除失败。

MySQL 重启产物记录容器启动时间由 `2026-09-09T15:06:07.158661777Z` 变为 `2026-09-09T15:08:13.752804434Z`，健康检查及重启后读取通过。同租户 16 个并发读取样本平均 72.498328ms、最大 93.552914ms；这是该 runner 的资格样本，不是生产性能或 SLA 承诺。

## 风险与回滚

时间准入点是根事务内取得屏障并检查的时刻；此前准入的事务可以在墙钟到期后完成，不承诺按提交瞬间取消。全局目录排他修改会阻塞商业写；新订阅/额度操作仍须遵守固定锁顺序。

关闭派生缓存使用 `Options.DisableEntitlementCache=true`，不能关闭权威校验或 Guard。保留来源、历史版本和审计，不用旧快照覆盖新限制。已经启用的租户不得直接退回不含屏障的旧版本；需受限版本或暂停入口。资格环境证明原子回滚、重新计算与重启持久性；没有执行生产迁移、生产回滚或生产负载测试。

## 集成与回读

实现 PR：#31；候选 head `074fa8cad39891afd41cabdd7910448e0f99541a`；PR 测试 merge ref `c8db5689dca8d328fcb6753a382e9c0bf7ba0e11`。正常合并得到业务 main `e5ce55414dde72c61f639ad3e0ffad7b86d46816`，合并时间 2026-09-09 15:05:38Z。主线资格使用该实际 SHA，而非重命名 PR 结果。

没有未解决 review thread；自动 Codex review 提示额度限制，不声称独立 APPROVE。本回执仅覆盖 CE-06，其他前端 PR 未混入。

证据提交 SHA 由 Git 历史体现，避免自引用。最终回执提交进入 main 后，既有 CE-06 工作流再次执行全部资格和 `python3 docs/commercial-entitlements/tools/check_round.py --task CE-06 --require-main`；须读取其实际 round.log 和最终 main ref 后才能结束本轮。该最终运行的不可变定位记录在交付回复/PR 评论，不伪填未来 run ID。

## 自检

scope：仅 CE-06；dependency：CE-05 完成且框架未变；quality：真实业务 main 证据非零、无失败/跳过、产物摘要和计数已核验；risks：保留时序语义和非生产限制；rollback：保留权威来源及限制，缓存优化可关闭，未声称生产回滚已演练。
