# CE-07 执行回执

## 范围与结果

完成 R3/CE-07 套餐版本、发布与适用资格，业务 PR #36 已正常合入 main。实际业务集成及验证 SHA：`69bd6672a76da40c04ade20a40dca4fcf3a3a2f4`。

本任务交付平台套餐编制与查询 API，不交付租户订阅。CE-08 默认开通、CE-09 套餐切换、支付、额度扣减、CE-20 字段输出保护、Vue、框架升级及生产部署均不在范围内。发布套餐本身不会给租户增加权益。

机器证明见 [CE-07-verification.json](CE-07-verification.json)，实现约定见 [CE-07-plan-versions.md](../decisions/CE-07-plan-versions.md)。本回执提交只增加两份证据并更新 tasks.json 的 CE-07 项；最终证据 main 仍需同轮实际通过资格及 --require-main，不能以写入 DONE 文本代替验证。

## 基线与依赖

起点 main：`6ea9f388dcbf40747c02ff4b33e96e1df8b56378`；Yunka 固定 `6ba99c1440dc6c9416f6afd08f3282e35fa5a3fb`，未升级。硬依赖 CE-02 为 DONE，业务集成 `ed73396d5205bb2e12c5c6bb2cb18b36bcba5424`，证据为 CE-02.md 与 CE-02-verification.json。基线 CE-01～06 均完成；CE-07 无外部条件，其他任务外部门槛未改写。

## 变更清单

独立 plan.proto 与显式来源登记；PlanManagement 的八个 RPC/REST 用例：创建首稿、克隆新版本、修改草稿、发布、停售、按版本读取、分页列版本、适用资格预检。权限分别为平台读取、编制、发布，并要求无租户平台主体；租户 owner 或角色管理员不能成为发布者。

Plan/PlanVersion 分离稳定编号、版本号、修订号与聚合版本。发布内容使用规范 JSON 摘要且不可覆盖；V2 必须新建，停售保留 V1 内容和引用。条款显式声明模块能力、零与无限额度、字段动作、期限、销售范围及独立 price_ref。

Application 实现封装在 planmanagement/internal/usecase；通过 typed ModuleCatalog child 在原根事务中读取当前目录，固定目录锁→套餐锁→版本/回执/审计顺序。配置校验、CAS、业务幂等及审计失败回滚使用既有 Executor/UoW。新增五张所属表、RESTRICT 引用及显式 SQL 迁移，未造订阅表或第二套事务引擎。

来源清单、商业映射 v4、客户端、OpenAPI、策略与运行时装配由锁定生成器产生；运行配置增加一个套餐 Application。业务变更共 44 个文件，无前端、框架锁或依赖版本变化。一次性生成脚本和写权限工作流已移除，正式资格工作流 contents:read。

## 验证命令与结果

实际执行环境为 GitHub Actions，Go 1.25.13、固定 Yunka 与工作流独占 MySQL 8.4。本交互容器未运行全量 Go/MySQL，负责下载原始产物、独立重算摘要/计数和运行 Python 证据检查，不冒充本地产品测试。

候选 head `d9144e6a8d5d8042c1286c62b99df97e79130f86` 的 15 条相关工作流均通过。PR 实际测试 merge-ref 为 `5e592f029aa493710f31df37f3b8932219dc8892`；业务 main 合并后重新运行，9 项检查全部 SUCCESS。

主线 CE-07 run `34375985236` / job `102548781866`，2026-09-09 16:19:27～16:22:19 UTC；artifact `10113978019` 已下载，ZIP SHA256 `2dded4241a36952089037c6437a404a5eac66ba2ff51458ea82f72b9d7e09d29`。summary SHA256 `93d6a89e1296839558138ab9b93cd45b71931090274d5969428fff68472ab3d6`。24 项文件哈希、10 组 JSONL 叶子计数逐项核对一致，源代码归档 commit 与业务 main 一致。

| 主线检查 | 实际结果 |
| --- | --- |
| TestCE07 专项 | 28 个叶子用例，29 个 pass 事件 |
| TestCE07MySQL | 11 个叶子用例，12 个 pass 事件 |
| 同组真实 MySQL race | 11 个叶子用例，12 个 pass 事件 |
| 数据库重启前／后 | 各 1 项通过 |
| CE-06／05／04／02 MySQL | 分别 11／5／7／2 个叶子用例通过 |
| 全量 Go | 175 个叶子用例，189 个 pass 事件 |
| 来源、workspace、make check、两次生成 | 通过；生成后工作区零漂移 |
| go vet、go build | 通过 |
| 计划回执／运行时闭环／计数器反向测试 | 分别 8／25／6 项通过 |

上述命令退出码均为 0。各组实际失败/跳过均为 0；专项、race、MySQL 与全量有重叠，不相加作为总测试数，无测试文件包单独统计。完整命令与日志定位见机器证明。

运行时 run `34375985085` / job `102548780285` / artifact `10113917870` 已下载；ZIP SHA256 `7572f89b5feed587b94a5b0eda236405c07aae96d8a93083fdf0db198e8a7b6a`。使用该主线生成 manifest 与归档 config/status/graph/diagnostics 重新执行原门禁得到 9 个应用、34 条唯一 HTTP 路径，RUNTIME_STOP_CHECK=PASS。

## 反例与故障验证

真实 REST/gRPC/MySQL 覆盖 V1/V2 隔离、停售旧读与新申请拒绝、分页、引用约束拒绝物理删除、内容被篡改后 DataLoss；未就绪/未知能力、依赖缺失、非法额度/字段、强制脱敏底线不能被套餐覆盖。平台编制者无发布权限、租户 owner 即使在测试库获同名字串权限仍不能成为平台主体，拒绝后无套餐副作用。

并发草稿 CAS 恰好一个成功、一个冲突；同业务请求重放不重复发布/审计，同键异载荷拒绝。审计故障有命中次数断言，发布/版本/回执全部回滚，解除故障后整请求重试成功。

通过旧 RR 视图及确定同步点验证：技术停用先提交，旧事务不能发布；发布先持屏障，技术停用的真实等待由 performance_schema.data_lock_waits 观察，先发布后停用且后续申请拒绝。数据库实际重启 StartedAt 从 `2026-09-09T16:19:46.451410058Z` 变为 `2026-09-09T16:21:47.923739288Z`，恢复后已发布版本仍可读。

本轮失败与修复保留：首次测试缺闭合括号；随后来源清单漏登记 plan.proto，被原生成门禁拒绝。run34373643961 已生成/编译但 Actions token 无权变更工作流，改由授权连接管理工作流、生成器只推源码且断言零工作流差异；run34374351059 成功，没有扩大 token 权限。

首轮真实资格 run34374586582/artifact10113393435 暴露 GORM current-read builder 复用造成套餐主表/版本表混用、SQL1054，五个顶层用例失败。修复 d9144e6 将两个查询 builder 分离，仍使用同一根事务与 SHARE 锁；没有修改任何业务断言，原套件复验通过。

B12 run34375103191 第一次在下载 Netflix/go-env 时出现 proxy.golang.org HTTP2 INTERNAL_ERROR。确认生成差异为空后对同一 SHA 正常重跑，job102547601817 的两次生成、契约与编译全部通过；没有通过改源码或降低门禁处理网络失败。历史失败日志和 PR 讨论保留。

## 风险与回滚

适用资格只是平台预检，不是订阅、开通或免检许可。sales_scope 是显式范围代码；CE-08/09 必须从可信业务事实确定范围并在最终根事务重查，不能直接信任租户自报范围。字段模板底线只是编制约束，不声明 CE-20 输出过滤已完成。price_ref 不表示已付费。

发布错误通过停售并创建新版本修正，不覆盖原版本、不删除引用/审计。必须先部署显式 SQL 迁移再启用新二进制；AutoMigrate 仅开发/资格环境。目录与套餐写有锁竞争，未进行生产负载或生产迁移/回滚演练。正常回滚不得关闭原权益/IAM Guard、放宽安全底线或破坏历史引用。

## 集成与回读

PR #36 于 2026-09-09 16:19:20 UTC 正常 squash 合并，业务集成 SHA 与机器证明 verified_commit 和 tasks.json integration_commit 均为 `69bd6672a76da40c04ade20a40dca4fcf3a3a2f4`。

原始主线资格产物保留 task_status=IN_PROGRESS / round_check=QUALIFIED_NOT_DONE，不改写证据生成时的真实状态。本三文件回执经唯一正常 PR 集成后，同轮再次读取最新 main、资格原始 summary/round.log 与 CE round receipts，实际执行 `check_round.py --task CE-07 --require-main` 才可结束；最终 run/commit 定位记录在交付回复及回执 PR 讨论，不写未来 PASS 或自身 SHA。

自动 Codex review 因额度受限未完成，不声称独立 APPROVE；未解决 review thread 为 0。CE-08 及后续仍未启动。

## 自检

scope：仅 CE-07，编制与资格而非订阅；dependency：CE-02 完成且框架锁未变；quality：实际 PR/业务 main 非零测试、失败修复及原始产物复核；risks：资格重新检查、锁竞争、非生产范围；rollback：停售/新版本修正、保留历史引用与原权限限制。
