# This delivery is an explicitly labelled UI preview. No production credentials belong in Vite variables.
VITE_DATA_MODE=demo
# Used only by the future authenticated API integration (see README).
VITE_API_BASE_URL=/api
